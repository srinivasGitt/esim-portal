import { KeyToDisplayPipe } from './key-to-display.pipe';

describe('KeyToDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyToDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
