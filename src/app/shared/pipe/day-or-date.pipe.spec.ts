import { DayOrDatePipe } from './day-or-date.pipe';

describe('DayOrDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DayOrDatePipe();
    expect(pipe).toBeTruthy();
  });
});
