import { ShowentriesPipe } from './showentries.pipe';

describe('ShowentriesPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowentriesPipe();
    expect(pipe).toBeTruthy();
  });
});
